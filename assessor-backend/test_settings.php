<?php define("WP_USE_THEMES", false); require("wp-load.php"); try { $settings = new Assessor_Settings(); $data = $settings->get_settings(); print_r($data); } catch (Throwable $e) { echo "Error: " . $e->getMessage(); }
